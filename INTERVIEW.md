# INTERVIEW.md — 高频追问与回答要点

围绕 BuildSage 设计决策的高频追问及回答要点（面试/答辩自述参考）。

## 1. 为什么做四通道，而不是一个 RAG 全包？

**要点**：装机场景的问题性质差异极大，用一条链路会两头不讨好。参数查询（"4070 的 TDP 是多少"）是**可查证的事实**，正确答案唯一，用向量检索去"猜"既慢又可能错，应该走结构化直查；兼容性判断（"650W 带得动 4090 吗"）是**规则可枚举的约束推理**，交给 LLM 自由发挥就是主动引入幻觉，应该走规则引擎；只有选购建议（"5000 元怎么配"）才是开放问题，需要检索+生成的 RAG。四通道把"确定性问题"和"开放问题"分开，各自用最合适的工具，这是本项目和普通 RAG Demo 的核心区别。

## 2. 参数直查为什么用 SQL 而不是向量检索？

**要点**：参数是结构化的，查"RTX 4070 的 TDP"本质是 `SELECT tdp_w FROM gpu WHERE name LIKE '%4070%'`，毫秒级且结果确定。向量检索是模糊语义匹配，对"4070"和"4070 Ti"可能召回混在一起，还会把数值切碎在 chunk 里。做法是让 LLM 用 `with_structured_output` 抽槽位（类别/型号/字段），再拼参数化 SQL 查库——LLM 只做"理解问题"，不做"记参数"，准确率和延迟都更好。实测参数直查准确率 94%+、延迟 1 秒内。

## 3. 兼容性为什么用规则引擎，不让 LLM 判断？

**要点**：兼容性是可枚举的硬约束：插槽相等、内存代匹配、功耗预算、显卡长度、PCIe 世代。这些用纯 Python 实现（R1~R5），结论 100% 可复现、可单测、可溯源到规则编号和具体数值。LLM 只负责把用户口语化的描述（"14900K 配 4090 用 650W 电源"）抽成结构化清单，判断交给规则引擎。这样做的好处：① 不会幻觉（LLM 可能算错 253+450+80）；② 可以自动生成金标（同一条规则既产答案又产真值）；③ 每条结论都能引用证据，用户看得懂。

## 4. 为什么用 LangGraph，而不是 LangChain 的 Agent 循环？

**要点**：LangGraph 只是编排工具，关键看编排的是什么。本项目是**确定性管道**：router → 四条通道之一 → 出口，节点单一职责、无循环、无 LLM 自主决策工具调用。参数和兼容性是确定性问题，上自主 Agent 循环只会增加延迟和不确定性。我另一个项目（旅行规划 Agent）用 LangGraph 编排的是**自主决策循环**（8 个工具、max_rounds 防死循环、多轮澄清）——同一框架两种用法，这个对比本身说明我理解"什么时候该用 Agent、什么时候不该"。

## 5. 混合检索怎么做的？为什么用 RRF？

**要点**：用 LangChain 的 `EnsembleRetriever` 把 `BM25Retriever`（稀疏、关键词）和 Chroma 向量检索（稠密、语义）融合。RRF（Reciprocal Rank Fusion）只按"排名倒数"求和，不看原始分数量纲——向量余弦相似度是 0~1，BM25 分数是无界实数，直接加权会被一方主导。RRF 是社区标准做法，EduRAG 里我手写过一遍，这次用 LangChain 标准组件实现，行为一致但可维护性更好。

## 6. 重排的价值和成本？

**要点**：向量/BM25 是"粗排"，CrossEncoder（bge-reranker-large）把 query 和文档拼在一起逐对打分，是"精排"，能显著提升 Top5 的相关性。成本是 CPU 环境下每对约 100~200ms，召回 50 条就要 5~10 秒。折中是把召回降到 20 条再重排到 5 条——精度损失很小（Recall@5 仍有 85%+），延迟砍掉一半以上。生产环境会上 GPU 或换更小的 reranker，或者用两阶段缓存（同一 query 的重排结果缓存）。

## 7. 结构化 RAG 怎么评估？金标为什么能自动生成？

**要点**：这是结构化数据的最大优势。参数类的真值就是规格库里的字段值，兼容类的真值是规则引擎对同一套配置的判定——**同一份结构化数据既能产题又能产答案**，`scripts/gen_eval_set.py` 自动生成 103 条金标（param 36 / compat 32 / rag 27 / reject 8），seed 固定可复现。指标上：路由分流正确率、参数准确率、兼容判定准确率都是**硬指标**（不需要 LLM 评委），只有攻略问答的 RAGAS 需要 LLM 打分。这比纯 RAG 项目"只有 Recall 和人工感觉"的评估扎实得多。

## 8. 规格数据从哪来？抽取踩了什么坑？

**要点**：CPU/GPU 从 Wikipedia 公开规格列表抽取（CC BY-SA），PassMark 榜单补性能分，主板/内存/电源人工整理，共 500 个型号。踩的坑：① Wikipedia 表格大量用 rowspan/colspan（型号跨行、表头跨列），直接按列头取值必然错位——写了个"网格展开器"把表还原成规则二维表再按列头语义取字段；② `find_col(headers, "a") or find_col(headers, "b")` 在返回索引 0 时是 falsy，会错误地走第二个分支，导致 Intel 品牌列丢失；③ 显存/TDP 在不同代际表格里列名和单位不一致，用"列头优先 + 行内正则扫描兜底"双策略。

## 9. 缓存一致性怎么取舍？

**要点**：答案缓存 TTL 1 小时、拒答 10 分钟，容许多端短暂不一致换响应速度；文档增删后按 category 清理对应前缀缓存，属于最终一致。缓存 key 是 `qa:ans:{category}:{md5(question)}`，Redis 不可用时降级进程内存。要注意的取舍：相同问题在不同会话里会命中同一缓存，多轮上下文（rewrite 后的 query）不参与缓存 key——这是为了命中率牺牲了一点上下文相关性，因为装机问答多数是独立问题。

## 10. QPS 上来之后瓶颈在哪？

**要点**：① 重排是 CPU 密集的串行环节（最大瓶颈），解法是 GPU、小模型或重排结果缓存；② 一次 RAG 问答串行 2 次 LLM 调用（路由分类 + 生成），可以把路由和槽位抽取合并成一次结构化输出；③ BM25 索引目前在进程内存、构建时读全表，文档量大要换 Elasticsearch/OpenSearch 或 MySQL 全文索引；④ LLM 调用可以并发（参数抽取和检索并行）；⑤ 答案缓存已经能挡掉重复问题。这些都在 README 的"后续优化"里列了。

## 11. 开发中印象最深的问题？

**要点**：① DeepSeek 不支持 OpenAI 的 `json_schema` 响应格式，`with_structured_output` 默认方法报 400，改成 `method="function_calling"` 才通——说明"OpenAI 兼容"并不等于全兼容；② langchain-huggingface 1.x 移除了 `HuggingFaceCrossEncoder`，我自己写了个 `BaseCrossEncoder` 适配器包住 sentence-transformers 的 CrossEncoder，15 行代码接回标准重排管道；③ MySQL 里 `memory_types` 存的是逗号分隔字符串，规则引擎里迭代字符串变成了逐字符 `D,D,R,5`，导致内存代数误判——这类"类型假设"的 bug 只能靠端到端测试发现。

## 12. 和另一个 Agent 项目怎么区分？

**要点**：BuildSage 打的是 **RAG 工程**牌（结构化+非结构化混合、评估驱动、延迟优化），TripAgent 打的是 **Agent 编排**牌（多工具自主决策、多轮澄清、外部 API 集成）。技术上两者都用了 LangGraph，但一个是确定性管道、一个是自主循环；框架栈上 BuildSage 用 LangChain 标准组件，另一个项目用 LangGraph + 自研工具层。面试里这两个项目回答的是不同问题："你会不会做检索质量与评估"和"你会不会做工具编排与状态管理"。
